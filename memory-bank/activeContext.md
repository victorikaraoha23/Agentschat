# Active Context

_Last updated: 2026-09-20, after commit `df1fa97`._

## Where things stand

The backend is scaffolded and runnable (`uv run pytest`, ruff, mypy, and `GET /health`); the
frontend is still a placeholder and no product feature exists yet. The full status table lives in
`progress.md`.

| Commit | Message |
|---|---|
| `df1fa97` | feat(backend): scaffold FastAPI application |
| `e1f126a` | docs: add agent context and rules files (Agents.md, .clinerules) |
| `ab15dad` | docs: initialize memory bank |
| `0890c0c` | chore: initialize monorepo with conventions |
| `dd56854` | Initial commit from scratch (an empty `readme.md`) |

## Current focus

Filling in the backend behind the healthy `/health` route: the Alembic environment and first
migration, the `db` / `db_commit` / `client` / `mock_llm` / `mock_redis` fixtures in
`tests/conftest.py`, then the credit service. Alongside that, scaffolding the Next.js app so
`tsc --noEmit`, `eslint`, and `vitest` have something to run against.

## Immediate next steps (proposed — not yet approved)

1. ~~**Backend scaffold.**~~ **Done** in `df1fa97` — uv project on Python 3.11, `src/{main,config,db}.py`,
   the package tree, `/health`, and a passing health test. The venv is `backend/.venv` and every
   command runs through `uv run` from `backend/`.
2. **First migration.** `users`, `workspaces`, `workspace_balances` — autogenerated then
   hand-reviewed, with `ALTER TABLE ... ENABLE ROW LEVEL SECURITY` on every table, verified via
   `upgrade head` → `downgrade -1` → `upgrade head`.
3. **Test harness.** `tests/conftest.py` with `db`, `db_commit`, `client`, `client_commit`,
   `mock_llm`, `mock_redis`, plus `tests/factories.py` and `tests/concurrency.py`.
4. **Credit service.** `src/services/credit_service.py` implementing the ledger contract *before*
   anything in the product spends a credit.
5. **Frontend scaffold.** Next.js App Router + TypeScript strict + ESLint + vitest + MSW, then
   `lib/api.ts`, `lib/sse.ts`, and `lib/idempotency.ts`.

Ordering rationale: (2)–(3) unblock every later test, and (4) must exist before chat or jobs can
charge anything.

## Open questions

- **Rename `Agents.md` to `AGENTS.md`?** It is tracked now (since `e1f126a`) but keeps its original
  mixed-case filename.
- **Local Postgres and Redis.** Which dev setup (Supabase CLI local stack vs. containerised
  Postgres/Redis) and how the test suite reaches it.
- **Which replica runs migrations** (`RUN_MIGRATIONS=1` on exactly one).
- **`frontend/.gitkeep`** is the last placeholder — delete it once real files land.
- **Branch strategy.** `origin/update-branch` exists; current work is committed straight to `main`.
- **Pushing.** `main` is 1 commit ahead of `origin/main` (`df1fa97` unpushed); confirm whether
  commits should be pushed as they land.

## Assumptions currently in force

- `Agents.md` outranks every other document, including this memory bank.
- The backend venv is `backend/.venv` (uv-managed, CPython 3.11, gitignored) and every backend
  command is expected to run through `uv run` from `backend/`.
- The file and folder names in `Agents.md` §3 remain the target for `frontend/`; `backend/` already
  matches that layout.
- "Tests ship in the same commit as the code" (`CONVENTIONS.md`, backend rule 10) applies from the
  first line of application code onward.

## How to keep this file honest

Update it at the end of any task that changes focus or opens/closes a question. When a "next step"
here is done, move it into `progress.md` rather than leaving it here.
